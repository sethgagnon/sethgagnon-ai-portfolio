

# Add Headshot Photo

Copy the uploaded headshot to the project and wire it up in the hero section.

## Steps

1. **Copy the image** to `public/seth-gagnon.jpg` — this is already the path referenced in `HeroSection.tsx`, so no code changes needed.

2. **Verify** — the Avatar component already points to `/seth-gagnon.jpg`, so the photo should appear immediately after copying.

